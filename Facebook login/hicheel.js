// neg function-> parametreeree array avaad -> for gvilgeed element bvriig ni console doh.

// const one = (arr) => {
//   for (let i = 0; i < arr.length; i++) {
//     console.log(arr[i]);
//   }
// };

// one([123, 2, 3]);

const array = (arr) => {
  arr.forEach((element) => {
    console.log(element);
  });
};
array([1, 2, 3, 4, 5]);
